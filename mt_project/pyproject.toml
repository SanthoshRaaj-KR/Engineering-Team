[project]
name = "mt_project"
version = "0.1.0"
description = "mt_project using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.14.4"
]

[project.scripts]
mt_project = "mt_project.main:run"
run_crew = "mt_project.main:run"
train = "mt_project.main:train"
replay = "mt_project.main:replay"
test = "mt_project.main:test"
run_with_trigger = "mt_project.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
